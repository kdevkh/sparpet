<body>

    <!-- <%- include('header.ejs') %>

    <h1 class="createpost">팔로우 유저 조회</h1>
    <% user.forEach(user => { %>
        <div class="profile">
            <div class="profileImage">아이디 : <%= user.id %></div>
            <div class="email">이메일 : <%= user.email %></div>
        </div>
    <% }) %>

</body> -->

  <%- include('header.ejs') %>

  <div class="my-followers-container">
    <% followers.forEach((follower) => { %>
    <div class="my-follower">
      <div class="id">유저아이디: <%= follower.id %></div>
      <div class="email">이메일: <%= follower.email %></div>
    </div>
    <% }); %>
  </div>
</body>

